<UserControl x:Class="PhotoDownloader.Views.ImageSlotView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:converters="clr-namespace:PhotoDownloader.Views.Converters"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Top"
             MinWidth="120">
    <UserControl.Resources>
        <converters:StringNullOrEmptyToVisibilityConverter x:Key="NullToVisibility" />
    </UserControl.Resources>

    <Border HorizontalAlignment="Stretch"
            VerticalAlignment="Top"
            Background="{DynamicResource ControlFillColorDefaultBrush}"
            BorderThickness="0"
            CornerRadius="8"
            Padding="5"
            SnapsToDevicePixels="True">
        <Grid HorizontalAlignment="Stretch"
              Margin="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="220" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Margin="0,0,0,8"
                       HorizontalAlignment="Left"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                       Text="{Binding SlotNumber, StringFormat=Слот {0}}" />

            <Border Grid.Row="1"
                    Margin="0,0,0,10"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Background="{DynamicResource ControlFillColorSecondaryBrush}"
                    CornerRadius="6"
                    ClipToBounds="True"
                    Padding="4">
                <Viewbox Stretch="Uniform"
                         StretchDirection="DownOnly"
                         HorizontalAlignment="Stretch"
                         VerticalAlignment="Stretch">
                    <Image Source="{Binding PreviewImage}"
                           Stretch="None"
                           RenderOptions.BitmapScalingMode="HighQuality" />
                </Viewbox>
            </Border>
            
            <ui:TextBox Grid.Row="2"
                        Margin="0,0,0,10"
                        HorizontalAlignment="Stretch"
                        MinHeight="36"
                        MaxHeight="36"
                        TextWrapping="Wrap"
                        AcceptsReturn="False"
                        VerticalContentAlignment="Center"
                        Text="{Binding Url, UpdateSourceTrigger=PropertyChanged}"
                        PlaceholderText="Введите URL изображения (https://…)" />

            <TextBlock Grid.Row="3"
                       Margin="0,10,0,8"
                       HorizontalAlignment="Stretch"
                       Text="{Binding StatusMessage}"
                       TextWrapping="Wrap"
                       Foreground="#FFFF6B6B"
                       Visibility="{Binding StatusMessage, Converter={StaticResource NullToVisibility}}" />

            <Grid Grid.Row="4"
                  HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="8" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <ui:Button Grid.Column="0"
                           HorizontalAlignment="Stretch"
                           HorizontalContentAlignment="Center"
                           Content="Старт"
                           Command="{Binding StartDownloadCommand}"
                           Appearance="Primary" />
                <ui:Button Grid.Column="2"
                           HorizontalAlignment="Stretch"
                           HorizontalContentAlignment="Center"
                           Content="Стоп"
                           Command="{Binding StopDownloadCommand}"
                           Appearance="Secondary" />
            </Grid>
        </Grid>
    </Border>
</UserControl>
